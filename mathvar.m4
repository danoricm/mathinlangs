# datavar.m4
define(`a', `5')
define(`b', `3')
define(`sum', eval(a + b))
define(`diff', eval(a - b))
define(`prod', eval(a * b))
define(`quot', eval(a / b))

5 + 3 = sum
5 - 3 = diff
5 * 3 = prod
5 / 3 = quot
